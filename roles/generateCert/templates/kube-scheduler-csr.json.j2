{
    "CN": "system:kube-scheduler",
    "hosts": [
     "127.0.0.1",
     {% for ip in MASTER_HOSTS %}
        {% if loop.index != loop.length %}
            "{{ ip }}",
        {% else %}
            "{{ ip }}"
        {% endif %}
     {% endfor %}
    ],
    "key": {
        "algo": "rsa",
        "size": 2048
    },
    "names": [
      {
        "C": "CN",
        "ST": "BeiJing",
        "L": "BeiJing",
        "O": "system:kube-scheduler",
        "OU": "dev"
      }
    ]
}
